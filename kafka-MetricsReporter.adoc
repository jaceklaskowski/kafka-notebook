== MetricsReporter

=== [[JmxReporter]] JmxReporter

`JmxReporter` is a metrics reporter that is always included in link:kafka-settings.adoc#metric_reporters[metric.reporters] setting with `kafka.consumer` metrics prefix.
